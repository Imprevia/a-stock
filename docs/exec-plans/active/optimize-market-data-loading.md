# 优化市场环境数据加载

## Stage

接口拆分、渐进加载与验证

## Status

进行中

## Acceptance

- 保留现有 `/api/market-environment` 聚合契约，避免破坏已有调用方。
- 新增可独立读取的核心指数与第 01 章扩展证据接口，使前端无需等待慢扩展数据即可展示指数内容。
- 前端先呈现核心行情，再异步补齐章节证据；章节加载失败不清空已经可用的指数数据。
- 日期切换和连续刷新不会让旧请求覆盖新日期结果。
- 后端测试、前端生产构建和 docs-contract full 通过。

## Completion Evidence

- 待实现后补充。

## Remaining Gaps

- 待实现后复核真实 provider 的端到端耗时；自动化测试只验证请求解耦与契约，不替代外部网络基准。

## Next Step

先更新产品规格、架构与 runbook 中的渐进加载契约，再实现后端分层缓存和前端两阶段请求。
