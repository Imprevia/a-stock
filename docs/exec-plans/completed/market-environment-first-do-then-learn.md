# 为市场环境 02 至 09 文档补充先做再学模块

## Stage（阶段）

文档练习流程补全

## Status（状态）

`completed`

## Scope（范围）

为 `搭建交易系统/01-如何判断市场环境/` 中编号 02 至 09 的 8 份正文补充“先做再学”模块。模块按各文档主题提供盘后记录、判断标签和后续验证项；量化版继续通过 `sourcePath` 指向这些原文，不调整既有规则 ID、计算逻辑或目录结构。

## Acceptance（验收）

- 8 份目标文档均包含可直接执行的“先做再学”模块。
- 每个模块明确数据口径、练习输出、分类或判断方式，以及下一交易日验证项。
- 练习内容不把观察标签直接写成交易指令，并保留数据不足或证据冲突的处理方式。
- 模块沿用原版 `01.` 文档的盘后练习定位，并与 02 至 09 各篇主题一一对应。
- `git diff --check` 与 `python scripts/check-docs-contract.py --mode=full` 通过。

## Completion Evidence（完成证据）

- 原版市场环境目录的 02 至 09 共 8 份正文均新增“先做再学”模块。
- 模块分别覆盖宽度卡、短线生态卡、分层亏钱效应卡、主线候选卡、容量进攻卡、事件验证卡、环境归类卡和综合判断卡。
- 每个模块均包含固定记录口径、练习输出、确认/失效条件或数据不足处理，并明确观察结论不能单独生成交易指令。
- `git diff --check` 通过。
- `python scripts/check-docs-contract.py --mode=full` 通过：代码 0、文档 1、plan 0。

## Remaining Gaps（剩余缺口）

量化版对应文档仍保持规则说明层定位，本次未修改；后续如需量化练习入口，应另行提出规则层需求。

## Next Step（下一步）

已完成并归档。后续根据连续练习记录中的具体卡点继续调整字段或示例。
