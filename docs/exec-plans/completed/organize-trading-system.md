# 整理交易系统知识库目录

## Stage（阶段）

目录整理与文档归档

## Status（状态）

`completed`

## Scope（范围）

将 `搭建交易系统/` 中按下载批次生成的重复主题子目录，整理为与“如何判断市场环境”一致的章节目录结构；不改正文内容，不删除唯一文档，仅合并重复路径并统一文件名。

## Acceptance（验收）

- 每个章节仅保留一个 `章节号-主题` 目录。
- 章节总览文件位于对应目录并命名为 `0-主题.md`。
- 章节正文按 `01.`、`02.` 等顺序直接位于对应章节目录。
- 不存在同主题的重复下载子目录或散落的章节总览文件。
- 原有 Markdown 文件内容保持不变；发现的 1 个重复下载副本仅保留一份，其他文档均完整保留。
- `python scripts/check-docs-contract.py --mode=full` 通过。

## Completion Evidence（完成证据）

已完成 11 个章节目录归并；整理后保留 70 个 Markdown 文件，较整理前减少的 1 个文件与现存副本 SHA-256 一致；其余内容总字节数与去重后的原始内容一致。未发现旧式编号文件名或重复哈希；目录清单与门禁结果见本轮任务记录。

验证：`python scripts/check-docs-contract.py --mode=full` 输出 `docs-contract: 通过`。

## Remaining Gaps（剩余缺口）

- 未发现内容冲突或重复文件；后续新增飞书导出内容需按本计划的章节目录规则归档。

## Next Step（下一步）

整理已完成；后续维护时保持每章单目录、总览 `0-主题.md`、正文 `NN.` 编号的结构。
