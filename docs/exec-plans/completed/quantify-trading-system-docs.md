# 重写交易系统量化版文档

## Stage（阶段）

量化规则文档重写与验证

## Status（状态）

`completed`

## Scope（范围）

完整保留 `搭建交易系统/`，新建同构的 `搭建交易系统-量化版/`，重写全部 70 篇 Markdown。量化版统一定义数据口径、公式、阈值来源、评分、否决条件、缺失处理和校准状态；不获取行情、不实现交易代码、不宣称经验阈值已经验证。

## Acceptance（验收）

- 原目录仍为 70 篇且内容哈希不变，量化版具有完全一致的 70 个相对路径。
- 每篇量化文档包含 YAML 元数据、输入口径、量化规则、输出、缺失处理和校准要求。
- 规则 ID 全库唯一，格式为 `QTS-章节-文档-序号`。
- 主观判断必须绑定指标和阈值，或明确标为不可直接量化并排除出评分。
- 修正量化版 `02-如何判断市场情绪/03.判断新旧热点切换.md` 的源内容错位，原文件保持不变。
- 更新量化目录索引、原目录入口、`AGENTS.md`、仓库指南和状态文档。
- `python scripts/check-docs-contract.py --mode=full` 通过。

## Completion Evidence（完成证据）

- 原版与量化版均为 70 篇 Markdown，相对路径差异为 0。
- 量化版共 327 个规则引用和 327 个唯一规则 ID；重复 ID、无 ID 文档和路径编号不匹配均为 0。
- 70 篇均具有统一元数据、输入、规则、缺失处理、校准和输出接口；结构错误为 0。
- 机器接口和公式中的 snake_case 命中为 0；`明显`、`强势`、`健康`、`活跃`、`可控` 等未量化判断词命中为 0。
- 标记为 `validated` 的规则行为 0，未把经验阈值误写成已验证结论。
- 原目录聚合 SHA-256 为 `7DBCF0945D12EBB64E686D31DD8D26990EC1C2BBFBBA2A593BD08779AEF0DC77`，与实施前基线一致。
- 热点切换量化文档包含成交份额迁移等重建规则，且不再包含错位的“所有事件都先过五问”正文。
- `git diff --check` 通过；仅报告 Git 的 LF/CRLF 工作区提示。
- `python scripts/check-docs-contract.py --mode=full` 输出 `docs-contract: 通过`。

## Remaining Gaps（剩余缺口）

- 本阶段没有获取历史行情或执行回测；所有经验阈值仍为 `needs-backtest`。
- 第10章定义了状态晋级门槛，后续需按章节准备数据、计入成本并进行样本外校准。

## Next Step（下一步）

从市场环境和市场情绪章节开始建立历史数据集，按第10章流程校准阈值；只有样本外、成本、版本和回滚证据全部满足后，才将具体规则升级为 `validated`。
