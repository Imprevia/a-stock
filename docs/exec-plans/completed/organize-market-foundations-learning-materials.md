# 整理指数、K线、成交量、估值和宏观基础学习资料

## Stage（阶段）

学习资料整合与知识库入口完善

## Status（状态）

`completed`

## Scope（范围）

在第 01 章现有“指数、趋势位置和成交额”正文中，整合指数、K 线、成交量、估值和宏观基础，形成从概念、计算口径、常见误区到联合判断、训练计划和复盘模板的完整入门资料。同步补充章节总览与量化版的人读口径说明，但不新增、删除或重排既有 `QTS-*` 规则 ID，不修改规则 YAML、API 或网页看板。

## Acceptance（验收）

- 原版正文完整覆盖指数、K 线、成交量、估值和宏观五个主题，并说明它们之间的传导关系与使用边界。
- 资料包含核心公式、A 股常用指数、K 线与量价判读、主要估值指标、宏观指标传导、数据口径陷阱、学习计划、练习题和复盘模板。
- 量化版明确区分“既有评分规则”和“教学参考口径”，不把未注册指标伪装为可执行规则。
- 原版和量化版保持现有一对一路径，330 个既有规则 ID 不变。
- `git diff --check` 和 `python scripts/check-docs-contract.py --mode=full` 通过，或明确记录环境阻塞。

## Completion Evidence（完成证据）

- 原版正文扩充为 16 个部分，覆盖指数、K 线、趋势、成交量/成交额、估值、宏观、联合判断、数据偏差、六周训练、盘后模板、自测答案、术语和官方来源。
- 原版与量化版章节总览已补充推荐学习顺序；量化正文版本更新为 `0.3`，明确教学字段不进入既有评分。
- 原版和量化版仍各为 70 篇 Markdown，量化版唯一 `QTS-*` 规则 ID 仍为 330 个。
- `trading-rules/`、`src/`、`apps/` 和 `tests/` 无改动。
- `git diff --check` 通过。
- `python scripts/check-docs-contract.py --mode=full` 通过：代码 0、文档 2、plan 0。

## Remaining Gaps（剩余缺口）

- 本次范围无剩余缺口。估值和宏观指标尚未进入机器规则，属于明确排除的后续范围。

## Next Step（下一步）

已归档。后续若要把估值或宏观指标接入看板和规则引擎，应单独建立规格、active plan、固定快照和回测证据。
