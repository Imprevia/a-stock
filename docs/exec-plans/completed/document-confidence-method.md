# 将置信度计算方法写回指数学习资料

## Stage（阶段）

置信度口径文档化

## Status（状态）

`completed`

## Scope（范围）

把原版学习资料中的人工置信度标准、看板联合确认的分支逻辑、量化版覆盖率阈值和“不等于上涨概率”的边界写清楚。保留工作区已有的次日验证内容，不修改行情计算代码、看板逻辑、规则 YAML 或既有 `QTS-*` 规则 ID。

## Acceptance（验收）

- 原版说明高、中、低、数据不足分别如何判断，并给出至少一个正向确认和一个背离示例。
- 量化版说明市场广度、趋势、成交确认和数据完整性如何共同影响置信度。
- 明确区分“结论证据置信度”和“未来上涨概率”，并说明高置信度也可能表示对某个强势判断的反驳很确定。
- 文档引用的阈值与当前实现一致，未把待回测阈值描述为已验证收益信号。
- `git diff --check` 与 docs-contract 完整门禁通过，或明确记录阻塞。

## Completion Evidence（完成证据）

- 已在原版学习资料 `搭建交易系统/01-如何判断市场环境/01.指数、趋势位置和成交额.md` 的盘后操作流程中写入置信度方法：先做数据完整性检查，再按指数同步性、市场宽度、MA20 趋势位置和成交确认判断高/中/低/数据不足，并补充正向确认、背离示例及“置信度不等于上涨概率”的边界。
- 已在量化版学习资料 `搭建交易系统-量化版/01-如何判断市场环境/01.指数、趋势位置和成交额.md` 中写入看板三个位置的置信度口径：跨指数联合确认、四问结论与五指数矩阵、第 01 章证据总览；补充覆盖率公式和 `ok` / `fallback` / `partial` / `missing` / `failed` 数据质量权重。
- 已同步更新 `docs/status.md`，记录置信度口径已文档化；未修改行情计算代码、看板逻辑、规则 YAML 或既有规则 ID。
- `git diff --check` 通过；`python scripts/check-docs-contract.py --mode=full` 通过（代码 0、文档 1、plan 0）；规则 ID 唯一性仍为 330 个。

## Remaining Gaps（剩余缺口）

- 文档层没有剩余缺口。当前阈值和覆盖率权重是可执行的解释口径，未来仍需通过固定快照回测校准其与次日结果的关系，不能直接视为收益或上涨概率。

## Next Step（下一步）

归档完成；后续如调整阈值或权重，应同步更新量化文档、实现和固定快照验证证据。
