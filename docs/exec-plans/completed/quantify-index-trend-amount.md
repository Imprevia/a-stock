# 量化“指数、趋势位置和成交额”的判断规则

## 目标

将 `搭建交易系统/01-如何判断市场环境/01.指数、趋势位置和成交额.md` 中的方向、趋势、位置、量价、广度和市场状态判断改为可计算、可复核、可回测的规则。

## 范围

包含：数据口径、公式、阈值、边界条件、缺失处理、综合评分、盘后模板与校准要求。

排除：业务代码、行情接口、回测实现及自动交易。

## Stage（阶段）

- 当前阶段：**文档量化改写**
- 阶段序列：审阅 → 规则设计 → 改写 → 门禁验证 → 回写

## Status（状态）

`completed`

## 事实源

- `搭建交易系统/01-如何判断市场环境/01.指数、趋势位置和成交额.md`
- `docs/trading-system-directory.md`
- `AGENTS.md`

## 阶段任务

- [x] 识别主观判断词及数据依赖
- [x] 写入可计算的指标、阈值和状态规则
- [x] 写入缺失值、边界和回测校准要求
- [x] 运行 docs-contract full gate
- [x] 更新完成证据与剩余缺口

## Acceptance（验收）

- 所有核心结论均能由文中公式、阈值或评分规则复算。
- “强、弱、同步、分化、突破、破位、滞涨、趋势、震荡、修复”等词均有明确定义。
- 文档明确数据覆盖率、缺失值、异常值和阈值校准规则。
- `python scripts/check-docs-contract.py --mode=full` 通过。

## Completion Evidence（完成证据）

- 已改写 `搭建交易系统/01-如何判断市场环境/01.指数、趋势位置和成交额.md`。
- 已量化指数同步、风格差、均线趋势、震荡、区间位置、突破/破位、量价组合、广度和综合状态。
- 覆盖检查通过：四项评分、缺失处理、主观词定义、边界条件和校准要求均为 PASS。
- `python scripts/check-docs-contract.py --mode=full` 通过：`docs-contract: 通过（代码 0 / 文档 1 / plan 0）`。
- `git diff --check` 通过。

## Remaining Gaps（剩余缺口）

- 初始阈值尚未通过历史样本外回测，不能视为永久参数。
- 当前任务只完成规则文档，尚未将全部指标实现为程序化计算模块。

## Next Step（下一步）

后续单独创建回测与参数校准计划；在实现前保持本文阈值为可执行基线，并在输出中标记数据质量。
