# 记录下一交易日验证项的推导方法

## Stage（阶段）

学习资料操作细化

## Status（状态）

`completed`

## Scope（范围）

针对用户对“下一交易日只验证”的数据、维度和思考过程疑问，在第 01 篇学习资料中记录完整推导方法。同步统一教学分类与看板正式口径，不修改行情计算、看板代码、规则 YAML 或既有 `QTS-*` 规则 ID。

## Acceptance（验收）

- 说明验证项来自哪些数据和维度。
- 提供可重复的选择优先级和不同市场状态映射。
- 每个验证项都能写出确认、失效和仍不明确三种结果。
- 明确第二天仍需重新运行完整分类。
- `git diff --check` 与 docs-contract 完整门禁通过。

## Completion Evidence（完成证据）

- 正文新增数据质量、指数方向、市场宽度、MA20 趋势位置和成交确认五维说明。
- 新增“数据不足 -> 反证 -> 中性 -> 持续性”的验证项选择顺序。
- 新增同步上涨、权重护盘、成长占优、普遍走弱、分化未定型和数据不足对应的验证项。
- 新增权重护盘与同步上涨的确认、失效和仍不明确示例。
- 市场宽度教学口径已统一为 `≥55%` 且中位数为正、`≤45%` 且中位数为负，其余为混合；指数同步口径统一为至少 4 个指数达到正负 `0.5%`。
- `git diff --check` 通过。
- `python scripts/check-docs-contract.py --mode=full` 通过：代码 0、文档 2、plan 0。

## Remaining Gaps（剩余缺口）

- 本次范围无剩余缺口。所有数值阈值仍属于工程定义和待回测研究口径，不代表收益已经验证。

## Next Step（下一步）

已归档。实际练习时连续记录验证项及次日的确认、失效或仍不明确结果。
