# 实现指数、位置与成交额组合判断

## Stage（阶段）

第 01 页第四部分组合规则与页面实现

## Status（状态）

`completed`

## Scope（范围）

实现 `搭建交易系统/01-如何判断市场环境/01.指数、趋势位置和成交额.md` 第四部分。后端基于指数 OHLC、均线、60 日位置和成交额比值输出六类明确组合及证据；第 01 章同时汇总市场强弱、阶段、资金认可和交易模式。前端新增组合判断区、四项输出和六类规则对照，不在浏览器中补算业务指标。

## Acceptance（验收）

- 每个指数返回 `combination` 对象，覆盖组合键、状态、语义、证据、交易模式和是否明确命中。
- 六类组合分别对应底部修复、上升趋势、突破确认、高位分歧、震荡轮动、趋势破坏；按风险优先级匹配，阈值全部写入量化说明和测试。
- 未命中明确组合时状态保持 `null`，数据缺失时返回“数据不足”，不得使用乐观兜底分类。
- 第 01 章返回市场强弱、阶段、资金认可和交易模式四项组合输出；市场广度缺失时明确标注待确认。
- 页面展示当前所选指数的组合状态和证据、四项市场输出以及六类规则对照，切换指数时同步更新。
- 后端全量测试、前端构建、桌面与 `390px` 移动浏览器 QA、docs-contract 完整门禁通过。

## Completion Evidence（完成证据）

- `classify_index_combination` 已按风险优先级实现趋势破坏、高位分歧、突破确认、底部修复、上升趋势和震荡轮动六类组合；未命中明确条件时返回 `key: unclassified`、`state: null` 和 `matched: false`，不使用兜底分类。
- 每个指数的 API 契约已新增 `combination`，`chapter01.combinationOverview` 已汇总市场强弱、阶段、资金认可、交易模式、置信度和证据；前端只消费后端结果，不在浏览器中计算业务阈值。
- 第 01 页已新增“当前指数”“四项输出”和“六类典型组合”区域。切换上证指数与深证成指后，标题和证据同步更新；深证成指实测显示 `60 日位置 24%`、`5 日成交额比值 1.03x`。
- 量化说明已更新至 `quantVersion: 0.2`，并同步产品规格、架构、runbook 和设计规范。该展示映射复用 `QTS-01-01-01` 至 `QTS-01-01-05`，未新增或重排规则 ID。
- `.venv/Scripts/python.exe -m pytest tests -q`：`49 passed`，仅保留既有 Starlette/httpx 弃用警告。
- `npm run build` 通过，仅保留既有大 chunk 提示；`git diff --check` 通过，仅有 CRLF 转换提示。
- 浏览器 QA 覆盖 `1440 x 900` 与 `390 x 844`：第四部分布局完整、无页面级横向溢出，所有可见文字不低于 `14px`；热更新前的历史错误在重新加载后没有新增。
- 常规 docs-contract full 仅因新建计划尚未被 Git 跟踪、归档后无法从 `git diff` 识别本次 active plan 而触发 Gate 2/3；按仓库逃生口使用 `SKIP_PLAN_GATE=1` 重新执行后通过完整门禁（代码 8 / 文档 6 / plan 0），并在此记录原因。

## Remaining Gaps（剩余缺口）

- 六类阈值仍为 `needs-backtest`，需要后续历史样本校准；这不阻塞本次可解释展示实现。

## Next Step（下一步）

归档到 `docs/exec-plans/completed/`；后续积累 500–750 个交易日快照，对六类组合的阈值和优先级进行回测校准。
