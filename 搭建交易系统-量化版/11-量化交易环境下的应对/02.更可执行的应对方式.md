---
title: "2.更可执行的应对方式"
sourcePath: "搭建交易系统/11-量化交易环境下的应对/02.更可执行的应对方式.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "daily-and-intraday"
lastCalibrated: null
---

# 2.更可执行的应对方式

## 目标与输出

根据波动、流动性、冲击成本和信号衰减调整订单、仓位、持有期和交易频率。

**组合判定：**执行参数只向更保守方向自动调整；恢复到基准参数必须满足连续观察期和成本恢复条件。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-11-02-01` | 订单规模倍率 | 订单 | min(1,用户最大成交占比/预计成交占比) | 下单前 | 固定公式；用户参数待校准 | 25% | 否 | defined |
| `QTS-11-02-02` | 限价保护 | 订单 | 允许滑点=max(最小报价单位,历史滑点P80) | 订单/250笔 | 经验初值，待回测 | 20% | 预计滑点超过策略优势时取消订单 | needs-backtest |
| `QTS-11-02-03` | 风险预算调整 | 账户 | 1-微观结构风险分/100，设置用户最小倍率 | 日/盘中 | 固定映射，倍率待校准 | 25% | 否 | needs-backtest |
| `QTS-11-02-04` | 持有期适配 | 策略 | 计划持有期/信号半衰期 | 研究/实时 | ≤1=100，1–1.5=75，1.5–2=25，>2=0；经验初值 | 15% | 否 | needs-backtest |
| `QTS-11-02-05` | 恢复条件 | 市场/策略 | 流动性、滑点、波动连续3个观察窗口回到P60以下 | 3窗口 | 满足=100，不满足=0；条件和权重均为经验初值，待回测 | 15% | 未恢复前不得提高订单与风险倍率 | needs-backtest |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "11-量化交易环境下的应对/02.更可执行的应对方式.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
