---
title: "6.连续亏损和账户回撤"
sourcePath: "搭建交易系统/07-仓位管理与风险控制/06.连续亏损和账户回撤.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "daily-and-per-trade"
lastCalibrated: null
---

# 6.连续亏损和账户回撤

## 目标与输出

用连续亏损、累计R、滚动回撤和恢复证据自动降低风险预算。

**组合判定：**风险倍率采用用户基准的1.0/0.5/0.25/0四档；触发项取最低倍率。默认阈值为经验初值，必须按策略历史分布校准。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-07-06-01` | 连续亏损 | 账户 | consecutiveLosingTrades | 滚动交易 | 0–2=100，3=50，4=25，≥5=0；经验初值 | 20% | 否 | needs-backtest |
| `QTS-07-06-02` | 滚动累计R | 账户 | sum(realized_R,last10Trades) | 10笔 | ≥0=100，-2R至0=75，-4R至-2R=50，-6R至-4R=25，<-6R=0；经验初值 | 25% | 否 | needs-backtest |
| `QTS-07-06-03` | 账户回撤 | 账户 | 1-equity/runningPeakEquity | 日 | ≤3%=100，3%–5%=75，5%–8%=50，8%–12%=25，>12%=0；经验初值 | 35% | 超过用户最大可承受回撤时暂停 | needs-backtest |
| `QTS-07-06-04` | 恢复条件 | 账户 | 复盘完成、违规归零、小样本模拟转正和环境恢复四项满足数/4 | 恢复期 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 20% | 恢复条件<75不得恢复满风险 | needs-backtest |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "07-仓位管理与风险控制/06.连续亏损和账户回撤.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
