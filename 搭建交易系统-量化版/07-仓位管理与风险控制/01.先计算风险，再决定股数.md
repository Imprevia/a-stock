---
title: "1.先计算风险，再决定股数"
sourcePath: "搭建交易系统/07-仓位管理与风险控制/01.先计算风险，再决定股数.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "per-trade"
lastCalibrated: null
---

# 1.先计算风险，再决定股数

## 目标与输出

根据账户允许损失和入场到止损的距离计算最大股数。

**组合判定：**计划股数=floor(允许单笔损失/(每股止损损失+预计每股费用与滑点))，再按交易单位、现金和流动性上限向下取整。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-07-01-01` | 允许单笔损失 | 账户 | accountEquity*baseRiskRate*riskPostureMultiplier | 下单前 | base_risk_rate为用户参数；不得把经验初值写成已验证值 | 30% | 否 | defined |
| `QTS-07-01-02` | 每股风险 | 交易 | abs(entry-stop)+feePerShare+slippagePerShare | 下单前 | 必须>0；固定公式 | 25% | ≤0时禁止计算仓位 | defined |
| `QTS-07-01-03` | 风险股数 | 交易 | floor(allowedLoss/riskPerShare/lotSize)*lotSize | 下单前 | 固定公式 | 25% | 否 | defined |
| `QTS-07-01-04` | 最终股数 | 交易 | min(风险股数,现金上限股数,流动性上限股数,集中度上限股数) | 下单前 | 固定公式 | 20% | 任一上限不可得时输出数据不足 | defined |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "07-仓位管理与风险控制/01.先计算风险，再决定股数.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
