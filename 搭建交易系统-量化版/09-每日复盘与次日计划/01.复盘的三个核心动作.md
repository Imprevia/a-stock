---
title: "1.复盘的三个核心动作"
sourcePath: "搭建交易系统/09-每日复盘与次日计划/01.复盘的三个核心动作.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "daily"
lastCalibrated: null
---

# 1.复盘的三个核心动作

## 目标与输出

分别量化还原现场、提取规律和迭代行动，避免三者混写。

**组合判定：**事实记录禁止使用事后不可见信息；规律必须有样本编号；行动必须有负责人、触发和完成状态。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-09-01-01` | 现场还原完整率 | 每日复盘 | 时间戳、当时数据、计划和实际动作已填项/必填项 | 盘后 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 40% | 否 | needs-backtest |
| `QTS-09-01-02` | 规律证据率 | 研究记录 | 带样本ID和对照组的规律数/提出规律总数 | 周/月 | 按250日滚动分位：≤P20=0，P20–P40=25，P40–P60=50，P60–P80=75，>P80=100；经验初值，待回测 | 30% | 否 | needs-backtest |
| `QTS-09-01-03` | 行动可执行率 | 改进项 | 含动作、触发、截止和验证字段的改进项/总改进项 | 周/月 | 按250日滚动分位：≤P20=0，P20–P40=25，P40–P60=50，P60–P80=75，>P80=100；经验初值，待回测 | 20% | 否 | needs-backtest |
| `QTS-09-01-04` | 行动完成率 | 改进项 | 按期完成并验证的改进项/到期改进项 | 月 | 按250日滚动分位：≤P20=0，P20–P40=25，P40–P60=50，P60–P80=75，>P80=100；经验初值，待回测 | 10% | 无原始记录的规律不得进入系统修改 | needs-backtest |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "09-每日复盘与次日计划/01.复盘的三个核心动作.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
