---
title: "4.动态优化的正确频率"
sourcePath: "搭建交易系统/09-每日复盘与次日计划/04.动态优化的正确频率.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "research"
lastCalibrated: null
---

# 4.动态优化的正确频率

## 目标与输出

用最小样本、冷静期、单变量修改和样本外验证限制频繁调参。

**组合判定：**单笔盈亏不得触发系统修改；只有达到第10章证据门槛并完成变更记录时才允许产生新版本。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-09-04-01` | 样本充分度 | 规则版本 | 自上次修改后的独立交易数 | 版本周期 | <30=0，30–59=25，60–99=50，100–199=75，≥200=100；经验初值 | 25% | 否 | needs-backtest |
| `QTS-09-04-02` | 修改冷静期 | 规则版本 | 异常出现至提出修改的交易日数 | 交易日 | <5=0，5–10=50，>10=100；经验初值 | 15% | 否 | needs-backtest |
| `QTS-09-04-03` | 单变量约束 | 变更 | 本次核心变量修改数 | 版本 | 1项=100，2项=50，>2项=0；固定审计规则 | 20% | 否 | defined |
| `QTS-09-04-04` | 样本外验证 | 变更 | 新规则样本外成本后期望及置信区间 | 样本外 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 30% | 无样本外验证不得发布 | needs-backtest |
| `QTS-09-04-05` | 回滚准备 | 变更 | 旧版本、基准、暂停和回滚条件完整 | 发布前 | 满足=100，不满足=0；条件和权重均为经验初值，待回测 | 10% | 不完整不得发布 | defined |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "09-每日复盘与次日计划/04.动态优化的正确频率.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
