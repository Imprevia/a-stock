---
title: "买入、持有与卖出规则"
sourcePath: "搭建交易系统/06-买入、持有与卖出规则/0-买入、持有与卖出规则.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "per-trade"
lastCalibrated: null
---

# 买入、持有与卖出规则

## 目标与输出

用交易计划、入场条件、结构失效、时间失效和组合风险统一买入、持有与卖出。

**组合判定：**执行优先级固定为组合风险>个股逻辑失效>结构止损>时间止损>止盈优化。没有计划编号、失效价和风险金额时禁止下单。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-06-00-01` | 计划完整率 | 交易计划 | 必需字段已填数/字段总数 | 下单前 | 100%=100，否则=0；固定门槛 | 25% | 不完整时禁止下单 | defined |
| `QTS-06-00-02` | 环境与模式适配 | 交易 | 环境分、候选分和模式分均值 | 盘前/盘中 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 25% | 否 | needs-backtest |
| `QTS-06-00-03` | 风险收益比 | 交易 | 计划目标收益/计划止损损失 | 下单前 | <1=0，1–1.5=25，1.5–2=50，2–3=75，≥3=100；经验初值 | 20% | 否 | needs-backtest |
| `QTS-06-00-04` | 持有有效性 | 持仓 | 逻辑、结构、时间和板块四项仍有效的比例 | 盘中/日 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 15% | 否 | needs-backtest |
| `QTS-06-00-05` | 退出触发 | 持仓 | 已触发的退出优先级及严重度 | 盘中/日 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 15% | 组合风险或逻辑失效触发时必须执行退出流程 | needs-backtest |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "06-买入、持有与卖出规则/0-买入、持有与卖出规则.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
