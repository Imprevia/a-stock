---
title: "回测、统计与系统迭代"
sourcePath: "搭建交易系统/10-回测、统计与系统迭代/0-回测、统计与系统迭代.md"
quantVersion: "0.1"
ruleStatus: "needs-backtest"
primaryFrequency: "research"
lastCalibrated: null
---

# 回测、统计与系统迭代

## 目标与输出

规定规则从研究想法到样本外、模拟和小风险实盘的晋级路径。

**组合判定：**任何规则默认needs-backtest；只有定义、数据、回测、样本外、成本和版本证据全部通过，才能标记validated。

## 输入与数据口径

- 市场类指标使用全A可交易样本，并按主板、创业板、科创板、ST和上市不足窗口样本分层。
- 价格使用前复权序列计算收益、均线和区间位置；成交额、换手率和涨跌停统计使用当日原始交易口径。
- 日频结论以收盘后数据为准；分钟级数据只在规则窗口明确要求时使用。
- 默认分位校准窗口为250个交易日，最少60个有效观测；不足时降低置信度或输出数据不足。

## 量化规则

| 规则ID | 规则 | 适用范围 | 指标与公式 | 窗口 | 阈值与得分 | 权重 | 否决条件 | 状态 |
|---|---|---|---|---|---|---:|---|---|
| `QTS-10-00-01` | 定义完整度 | 规则 | 输入、公式、阈值、动作、失效、缺失处理已定义项/6 | 版本 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 15% | 否 | needs-backtest |
| `QTS-10-00-02` | 数据质量 | 研究数据 | 覆盖、复权、停牌、涨跌停、幸存者和未来函数检查通过率 | 样本 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 20% | 未来函数或样本泄漏直接否决 | needs-backtest |
| `QTS-10-00-03` | 样本内表现 | 规则 | 第02篇核心指标得分 | 训练样本 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 20% | 否 | needs-backtest |
| `QTS-10-00-04` | 样本外表现 | 规则 | 成本后期望及置信区间 | 验证样本 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 30% | 样本外期望≤0不得validated | needs-backtest |
| `QTS-10-00-05` | 执行与版本证据 | 规则 | 模拟、小风险实盘、版本和回滚记录完整度 | 发布周期 | 按预先列明的五档边界计0/25/50/75/100分；经验初值，待回测 | 15% | 否 | needs-backtest |

## 数据缺失与异常处理

- 分母为0、行情无效或样本归属不明时，该指标记为`null`，不得填0代替。
- 有效指标覆盖率≥90%才允许高置信度；70%–89%最高为中；50%–69%最高为低；低于50%输出`insufficient`。
- 对剩余有效指标重新归一权重；涉及风险否决的输入缺失时保留风险提示，不按安全处理。
- 极端值保留原值用于风险识别；用于分位和均值时同时输出稳健统计，避免单点污染。

## 校准与验证

- 本篇所有`needs-backtest`阈值均为经验初值，必须记录训练区间、样本外区间、交易成本和规则版本。
- 至少比较固定阈值与滚动分位阈值，并按市场环境、板块、流动性或交易模式分组检查稳定性。
- 未满足第10章样本外、成本后期望、版本和回滚要求前，不得将状态升级为`validated`。

## 输出接口

```yaml
asOf: YYYY-MM-DD
document: "10-回测、统计与系统迭代/0-回测、统计与系统迭代.md"
score: 0-100
state: string
confidence: high | medium | low | insufficient
coverage: 0-1
triggeredRules: []
vetoes: []
missingInputs: []
confirmation: string
invalidation: string
ruleVersion: "0.1"
```

## 判断边界

不能从公开行情可靠观察的主体意图不进入评分，只保留为待核实证据。

本文是量化研究规则草案，不构成投资建议。
