apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: market-environment-postgresql-data
  namespace: a-stock
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
