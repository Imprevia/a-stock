## 1. 指数卡复制交互

- [x] 1.1 重构第 01 页指数卡的交互结构，保留非复制区域的指数选择行为，并为每张卡的收盘指数值和涨跌幅提供独立、可键盘操作的复制控件；验证既有指数切换测试仍通过且复制不会改变选中状态
- [x] 1.2 实现统一的复制辅助逻辑：优先 Clipboard API，必要时执行受控 fallback，并提供逐控件成功/失败状态和 `aria-live` 通知；验证成功、API 缺失、权限拒绝三种场景均不误报
- [x] 1.3 调整复制控件的视觉与响应式样式，确保 14px 最小字号、焦点可见、390px 页面无横向溢出；验证桌面和移动视口的卡片布局与状态提示

## 2. 默认交易日

- [x] 2.1 在研究看板首次核心数据加载边界同步后端返回的有效 `asOf`，仅归一化非交易日的初始候选日期，并保留用户手动选择；验证初始回退、初始命中和手动日期三种行为不会触发额外请求
- [x] 2.2 补充日期工具或 App 组件测试，覆盖 15:00 截止规则、周末/节假日有效日期回退和手动选择不被覆盖；验证 `npm run test --prefix apps/market-environment-dashboard` 通过

## 3. 文档与验收

- [x] 3.1 更新市场环境产品规格、设计规范、架构和运行手册，记录独立复制语义、剪贴板失败边界、初始交易日归一规则及浏览器验收要求；验证文档链接和内容与规格增量一致
- [x] 3.2 运行前端完整测试与生产构建，并执行 `python scripts/check-docs-contract.py --mode=full`、`git diff --check`；将命令结果、截图/浏览器检查和剩余缺口回填对应 active plan
